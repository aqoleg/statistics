<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="space.aqoleg.statistics">
    <application android:allowBackup="false" android:label="@string/label" android:icon="@mipmap/icon"
        android:theme="@style/day">
        <activity android:name=".ActivityMain" android:label="@string/label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>